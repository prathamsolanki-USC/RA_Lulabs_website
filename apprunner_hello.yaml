version: 1.0
runtime: python3
build:
  commands:
    build:
      - echo "=== HELLO WORLD BUILD ==="
      - python --version
      - pip install -r requirements_hello.txt
      - echo "=== BUILD COMPLETE ==="
run:
  runtime-version: 3.11
  command: python hello_world.py
  network:
    port: 8000
